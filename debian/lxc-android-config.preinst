#!/bin/sh

set -e

file=/usr/share/polkit-1/actions/org.freedesktop.NetworkManager.policy
divertto="${file}.orig"
dpkg-divert --package lxc-android-config --add --rename --divert \
	"$divertto" "$file"

#DEBHELPER#
